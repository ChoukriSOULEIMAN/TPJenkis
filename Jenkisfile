pipeline {
    agent any

    environment {
        // Voici les variables d'environnement
        CONTAINER_ID = ''
        SUM_PY_PATH = 'sum.py' 
        DIR_PATH = '.' 
        TEST_FILE_PATH = 'test_variables.txt' 
    }

}
